apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "low-code-vinyl.fullname" . }}-test-connection"
  labels:
    {{- include "low-code-vinyl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "low-code-vinyl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
